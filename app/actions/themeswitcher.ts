// 'use server';

// import { cookies } from 'next/headers';

// export const themeSwitcher = async (theme?: string) => {
//   if (!theme) return;
//   (await cookies()).set('theme', theme, { path: '/', httpOnly: false });
//   return theme;
// };
