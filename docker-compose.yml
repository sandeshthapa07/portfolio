services:
  web:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: nextjs-dev

    ports:
      - "3000:3000"

    env_file:
      - .env

    environment:
      NODE_ENV: development

    restart: unless-stopped
